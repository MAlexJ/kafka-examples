spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: test-group
test:
  topic: embedded-test-topic

cloud:
  kafka:
    topic: embedded-test-topic
    topic_input: embedded-test-input
    topic_output: embedded-test-output